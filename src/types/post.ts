export interface IPost {
  slug: string
  date: string
  thumbnail: string
  title: string
  description: string
  tags: string[]
  yields: string
  ingredients: string[]
  directions: string[]
  tips: string[]
}
